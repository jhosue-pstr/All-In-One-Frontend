name: SonarCloud Frontend

on:
  push:
    branches: [ "main" ]
  pull_request:

jobs:
  Analysis:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: SonarCloud Scan Frontend
        uses: SonarSource/sonarqube-scan-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: https://sonarcloud.io
        with:
          args: >
            -Dsonar.projectKey=jhosue-pstr_All-In-One-Frontend
            -Dsonar.organization=jhosue-pstr
            -Dsonar.sources=src
            -Dsonar.exclusions=node_modules/**,dist/**,public/**

      # # CNES REPORT - Deshabilitado temporalmente por incompatibilidad con SonarCloud API v8
      # # Revisar: https://github.com/cnescatlab/sonar-cnes-report/releases
      # - name: Setup Java for CNES Report
      #   uses: actions/setup-java@v4
      #   with:
      #     distribution: temurin
      #     java-version: 21
      #
      # - name: Download CNES Report JAR
      #   run: |
      #     curl -sL -o sonar-cnes-report.jar \
      #       "https://github.com/cnescatlab/sonar-cnes-report/releases/download/5.0.4/sonar-cnes-report-5.0.4.jar"
      #
      # - name: Generate CNES Report
      #   run: |
      #     java -jar sonar-cnes-report.jar \
      #       -t ${{ secrets.SONAR_TOKEN }} \
      #       -s https://sonarcloud.io \
      #       -p jhosue-pstr_All-In-One-Frontend \
      #       -o ./cnes-report
      #
      # - name: Upload CNES Report
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: cnes-report-frontend
      #     path: ./cnes-report/
